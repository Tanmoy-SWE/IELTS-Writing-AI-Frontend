import React, { useState } from 'react';
import './Note.css'

function Note(){
    return (
        <form>
        <fieldset><legend>Note</legend>
        <p>
            Thank you for using our IELTS Writing App. You can set your own questions and answer accordingly, then see your score instantly.    
        </p>
        </fieldset>
        </form>
    );
}

export default Note;